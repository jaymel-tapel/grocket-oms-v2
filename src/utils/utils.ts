export const isAuth = () => {
  // try {
  //   const access_token = localStorage.getItem("access_token");
  //   if (access_token) {
  //     return true;
  //   }
  //   return false;
  // } catch (e) {
  //   return false;
  // }

  // uncomment later above code
  const test = 1 + 1;
  if (test === 2) return true;
  return false;
};
