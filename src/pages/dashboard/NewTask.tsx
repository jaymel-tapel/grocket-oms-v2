import TaskForm from "../../components/dashboard/tasks/TaskForm";

const NewTask = () => {
  return (
    <div>
      New task / Update task page component. This is the component where you'll
      add '/Dashboard/Add Task'
      <TaskForm />
    </div>
  );
};

export default NewTask;
